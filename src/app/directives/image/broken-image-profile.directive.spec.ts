import { BrokenImageProfileDirective } from './broken-image-profile.directive';

describe('BrokenImageProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new BrokenImageProfileDirective();
    expect(directive).toBeTruthy();
  });
});
